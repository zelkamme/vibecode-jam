

# VibeCode Jam 🎸

**VibeCode Jam** — это современная платформа для автоматизации технических собеседований. Система позволяет проводить полный цикл оценки кандидата: от проверки Soft Skills и теоретических знаний до выполнения практических задач с написанием кода в реальном времени.

Платформа отличается стильным интерфейсом (Glassmorphism), встроенной системой запуска кода в изолированных контейнерах и модулем Anti-Cheat телеметрии.

## ✨ Основные возможности

### 👨‍💻 Для Кандидатов
*   **Многоэтапное собеседование:**
    1.  **Психологический тест (Soft Skills):** Quiz-формат для оценки поведения в команде.
    2.  **Теоретическое интервью:** Чат-интерфейс с эмуляцией собеседника (Simulated AI).
    3.  **Live Coding:** Полноценная IDE (Monaco Editor) с поддержкой Python.
*   **Code Runner:** Безопасный запуск кода и вывод результатов терминала (stdout/stderr).
*   **Личный кабинет:** Загрузка резюме (PDF) и выбор вакансии.
*   **Отчет:** Мгновенное получение результатов после завершения.

### 🕵️ Для HR и Рекрутеров
*   **Дашборд:** Управление списком кандидатов, фильтрация, сортировка.
*   **Конструктор Вакансий:** Создание вакансий с привязкой к уровню (Junior/Middle/Senior) и стеку.
*   **Конструктор Задач:**
    *   Создание задач по кодингу (с предзаполненным кодом).
    *   Создание теоретических вопросов.
    *   Настройка окружения (Basic Python / Data Science).
*   **Детальные отчеты:** Просмотр баллов и **Anti-Cheat метрик** (потеря фокуса, уход курсора, copy-paste).

## 🛠 Технический стек

### Frontend
*   **React** (Vite)
*   **React Router** — навигация и защита роутов.
*   **Monaco Editor** — редактор кода (как в VS Code).
*   **Axios** — взаимодействие с API.
*   **CSS Modules** — кастомный Glassmorphism дизайн без использования UI-библиотек.

### Backend
*   **FastAPI** (Python) — высокопроизводительный REST API.
*   **SQLModel** (SQLAlchemy + Pydantic) — ORM для работы с БД.
*   **SQLite** — база данных (хранится в файле `vibecode.db`).
*   **Docker SDK** — для запуска пользовательского кода в изолированных контейнерах.

## 🚀 Установка и запуск

Для работы проекта требуются **Python 3.10+**, **Node.js** и **Docker** (обязательно запущенный).

### 1. Настройка Backend

Перейдите в папку с бэкендом (предполагается, что файлы `main.py`, `models.py` и т.д. лежат в `backend/`):

```bash
cd backend

# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate
# Активация (Mac/Linux)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Заполнение базы данных тестовыми вопросами (ОБЯЗАТЕЛЬНО ПРИ ПЕРВОМ ЗАПУСКЕ)
python seed_questions.py

# Запуск сервера
uvicorn main:app --reload --port 8000
```

> **Важно:** Убедитесь, что Docker Desktop запущен, иначе выполнение кода работать не будет.

### 2. Настройка Frontend

Перейдите в папку с фронтендом (где лежат `package.json`, `src/`):

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск режима разработки
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

## 🔐 Доступы (Credentials)

В текущей версии аутентификация упрощена для демонстрации:

*   **HR (Рекрутер):**
    *   Вход: Нажмите "Войти как HR"
    *   Пароль: `admin`
*   **Кандидат:**
    *   Вход: Нажмите "Я Кандидат" -> Регистрация (любое имя).

## 📂 Структура проекта

```text
.
├── backend/
│   ├── main.py              # Точка входа API, роуты
│   ├── models.py            # Модели БД (User, Question, Vacancy, Report)
│   ├── database.py          # Подключение к SQLite
│   ├── seed_questions.py    # Скрипт для начального наполнения БД
│   ├── requirements.txt     # Python зависимости
│   └── uploads/             # Папка для загруженных резюме
├── frontend/
│   ├── src/
│   │   ├── components/      # (Опционально) переиспользуемые компоненты
│   │   ├── CodingInterface.jsx  # IDE с терминалом и чатом
│   │   ├── TaskBuilder.jsx      # Конструктор задач для HR
│   │   ├── VacancyBuilder.jsx   # Конструктор вакансий
│   │   ├── HrDashboard.jsx      # Таблица кандидатов
│   │   ├── ...                  # Остальные страницы
│   │   ├── App.css              # Глобальные стили (Glassmorphism)
│   │   └── App.jsx              # Роутинг
│   └── package.json
└── README.md
```

## 🛡 Anti-Cheat Система

Система собирает телеметрию во время прохождения теста кандидатом:
1.  **Focus Lost:** Сколько раз вкладка с тестом теряла фокус (alt-tab).
2.  **Mouse Left Window:** Сколько раз курсор покидал область окна браузера.
3.  **Large Pastes:** Детекция вставки больших кусков кода (защита от Ctrl+C / Ctrl+V готовых решений).

Эти данные агрегируются и вычисляют **Integrity Score** (показатель честности), который видит HR в отчете.

## 🐳 Изоляция кода (Docker)

При нажатии кнопки "Run Code" на фронтенде:
1.  Код отправляется на бэкенд.
2.  Бэкенд создает временный файл.
3.  Через Docker SDK запускается контейнер (`python:3.12-alpine` или `slim` для DS).
4.  Контейнер выполняется с ограничением ресурсов (CPU/RAM) и без доступа к сети.
5.  Логи (stdout/stderr) возвращаются на фронтенд.

---
