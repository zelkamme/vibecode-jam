Конечно! Вот профессиональный и подробный файл `README.md` для твоего проекта. Он включает описание, стек технологий, инструкцию по установке и решение частых проблем (которые мы с тобой исправляли).

Создай файл **`README.md`** в корне проекта и вставь туда этот текст:

***

# 🤖 VibeCode Jam — AI Платформа для технических собеседований

**VibeCode Jam** — это автоматизированная система для проведения технических собеседований без участия живого рекрутера. Система проверяет Soft Skills, теоретические знания и навыки программирования, используя искусственный интеллект.

Включает в себя песочницу для безопасного запуска кода (Code Runner) и генерацию отчетов по кандидатам.

![Status](https://img.shields.io/badge/Status-MVP-success)
![Docker](https://img.shields.io/badge/Docker-Enabled-blue)
![FastAPI](https://img.shields.io/badge/Backend-FastAPI-009688)
![React](https://img.shields.io/badge/Frontend-React%20%2B%20Vite-61DAFB)

## ✨ Возможности

*   **Мультиязычность:** Поддержка интервью для Python, JavaScript, Java, C++, Go.
*   **Soft Skills Check:** Ситуационные вопросы для проверки поведения и командной работы.
*   **AI Теория:** Умный чат-бот задает вопросы, оценивает ответы и задает уточняющие вопросы (Follow-up).
*   **Code Runner:** Встроенная IDE (Monaco Editor) с безопасным запуском кода в изолированных Docker-контейнерах.
*   **AI Code Review:** Автоматический анализ кода (стиль, функциональность) и генерация Unit-тестов.
*   **Аналитика:** Формирование итогового отчета с баллами и саммари по кандидату.
*   **Анти-чит (Integrity):** Отслеживание потери фокуса и копипаста.

## 🛠 Технический стек

*   **Frontend:** React, Vite, TailwindCSS (предположительно), Monaco Editor.
*   **Backend:** Python 3.12, FastAPI, SQLModel (SQLite).
*   **AI & Logic:** OpenAI API (или совместимые), LangChain (опционально), кастомные промпты.
*   **Database & Cache:** SQLite (хранение данных), Redis (кэширование контекста диалогов).
*   **Infrastructure:** Docker, Docker Compose (оркестрация), Docker-in-Docker (для запуска кода пользователей).

## 🚀 Установка и запуск

### Предварительные требования
*   Установленный [Docker Desktop](https://www.docker.com/products/docker-desktop).
*   Git.
*   Ключ OpenAI API (или совместимого сервиса).

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/zelkamme/vibecode-jam.git
cd vibecode-jam-main
```

### Шаг 2: Настройка переменных окружения

В файле `backend/main.py` (или создайте `.env` файл в папке `backend`) убедитесь, что указан ваш API ключ:

```python
API_KEY = "sk-..." 
BASE_URL = "..." # Если используете кастомный эндпоинт
```

### Шаг 3: Запуск через Docker Compose

В корне проекта выполните команду:

```powershell
docker compose up --build
```

*Первый запуск может занять несколько минут, пока скачиваются образы.*

### Шаг 4: Доступ к приложению

После успешного запуска откройте в браузере:

*   **Веб-интерфейс:** [http://localhost:3000](http://localhost:3000)
*   **API Документация (Swagger):** [http://localhost:8000/docs](http://localhost:8000/docs)

---

## 📂 Структура проекта

```text
vibecode-jam/
├── backend/                # Бэкенд на FastAPI
│   ├── llm/                # Логика работы с AI (промпты, генераторы)
│   ├── database.py         # Настройки БД
│   ├── models.py           # SQLModel схемы
│   ├── main.py             # Точка входа API
│   ├── seed_questions.py   # Скрипт наполнения базы вопросами
│   └── Dockerfile
├── frontend/               # Фронтенд на React + Vite
│   ├── src/                # Компоненты и страницы
│   └── Dockerfile
├── docker-compose.yml      # Оркестрация контейнеров
└── README.md
```

## 🐛 Возможные проблемы и решения

### 1. Ошибка сети при сборке (`TLS handshake timeout`)
Если Docker не может скачать образы:
*   Зайдите в настройки Docker Desktop -> Docker Engine.
*   Добавьте DNS Google:
    ```json
    "dns": ["8.8.8.8", "1.1.1.1"]
    ```
*   Перезапустите Docker.

### 2. Ошибка Code Runner (`No such file or directory`)
Если при запуске кода возникает ошибка с путями:
*   Убедитесь, что в `docker-compose.yml` у сервиса `backend` прописан volume:
    ```yaml
    volumes:
      - code_runner_shared:/tmp/shared
    ```
*   Попробуйте пересоздать контейнер бэкенда:
    ```bash
    docker compose up -d --no-deps --build backend
    ```

### 3. База данных пуста / Нет вопросов
Вопросы должны заливаться автоматически при старте. Если их нет:
*   Зайдите в контейнер бэкенда:
    ```bash
    docker exec -it backend sh
    ```
*   Запустите скрипт вручную:
    ```bash
    python seed_questions.py
    ```

---

## 👨‍💻 Авторы

**Zelkamme** - [GitHub Profile](https://github.com/zelkamme)

**Cesar Pronin** - [GitHub Profile](https://github.com/MexIvanov)

**Александр Сальный** - [GitHub Profile](https://github.com/debervill)

---
*Проект создан в рамках хакатона*